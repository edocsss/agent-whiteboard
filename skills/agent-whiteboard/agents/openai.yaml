interface:
  display_name: "Agent Whiteboard"
  short_description: "Publish agent artifacts as capability URLs"
  default_prompt: "Use $agent-whiteboard to publish this artifact safely and return its public URL."
